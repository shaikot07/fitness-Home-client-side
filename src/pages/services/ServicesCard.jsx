import React from 'react';

const ServicesCard = ({data}) => {
      return (
            <div>
                  card for service{data.name}
            </div>
      );
};

export default ServicesCard;